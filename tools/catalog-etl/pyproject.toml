[project]
name = "catalog-etl"
version = "0.0.0"
description = "Build-time ETL for the derived star catalog. Never ships to the player."
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
dependencies = []

[dependency-groups]
dev = ["ruff>=0.8", "pytest>=8"]

[tool.ruff]
line-length = 100
